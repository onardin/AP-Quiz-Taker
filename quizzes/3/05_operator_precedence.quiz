quiz_title="C Operator Precedence Quick-Fire"
quiz_questions=(
    "Which has higher precedence: * (dereference) or ++ (postfix)?"
    "True/False: && binds more tightly than ||."
    "Given x = 2; y = 3; what does x + y << 1 evaluate to?"
    "In the expression a = b = c = 0, which assignment executes first?"
)
quiz_answers=(
    "++ (postfix)"
    "True"
    "10"
    "c = 0 (rightmost)"
)
