quiz_title="Command-Line Args & String Arrays"
quiz_questions=(
    "What is argv[0] traditionally?"
    "argc counts how many arguments *excluding* what sentinel?"
    "Which function converts a numeric string to int?"
    "Given ./prog 24 96, what character does *(*(argv+2)+1) print?"
)
quiz_answers=(
    "Program name"
    "The final NULL pointer"
    "atoi"
    "6"
)
