quiz_title="Functions & Scope"
quiz_questions=(
    "Begin a function definition named 'foo' (type only the first line)."
    "Keyword that creates function‑local variables."
    "If a function echoes text with no redirection, to which stream does it go by default?"
)

quiz_answers=(
    "foo() {"
    "local"
    "stdout"
)
