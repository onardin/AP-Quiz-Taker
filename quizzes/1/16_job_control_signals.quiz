quiz_title="Job Control & Signals"
quiz_questions=(
    "Builtin that resumes a stopped job **in the background**."
    "Command that removes a job from the job table so it won’t receive SIGHUP on logout."
    "Signal name (not number) sent by 'kill -9'."
    "Default signal sent by 'kill PID' with no options."
    "Key combination that sends SIGTSTP to the current foreground job."
)

quiz_answers=(
    "bg"
    "disown"
    "SIGKILL"
    "SIGTERM"
    "ctrl+z"
)
