#!/usr/bin/env bash
quiz_title="Segmentation Faults"
quiz_questions=(
    "What is the most common cause of a segmentation fault?"
    "Dereferencing a NULL pointer is reported as an invalid read at what address?"
    "Attempting to write to which memory segment will cause a segfault with strcpy?"
)

quiz_answers=(
    "Dereferencing a null pointer"
    "0x0"
    "Read‑only"
)
